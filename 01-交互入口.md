# 01 - 交互入口(Interaction Entry)

> 用户与 AI 系统的接触面。涵盖 CLI 终端、WebUI 网页、Widget 组件、Copilot 副驾驶、Chatbot 对话机器人、ASR/TTS 语音交互七大入口形态,以及 IDE、API、SDK 等扩展形态。所有内容已对照 2025-2026 年官方文档与社区实践整理。

---

## 📚 本章专业词汇速查表

| 英文术语 | 中文译名 | 一句话解释 |
|---------|---------|-----------|
| CLI | 命令行接口 | 通过文本命令与系统交互的终端界面 |
| WebUI | 网页用户界面 | 在浏览器中运行的图形化交互界面 |
| Widget | 嵌入式组件 | 嵌入到其他应用中的轻量 AI UI 组件 |
| Copilot | 副驾驶 | IDE 内辅助编码的 AI 工具 |
| Chatbot | 对话机器人 | 以对话为核心交互模式的 AI 应用 |
| ASR | 自动语音识别 | 将语音转换为文字(Speech-to-Text) |
| TTS | 语音合成 | 将文字转换为语音(Text-to-Speech) |
| Agent | 智能体 | LLM 自主循环、调用工具完成任务的系统 |
| Approval Mode | 审批模式 | 控制工具执行前是否需用户确认的机制 |
| Sandbox | 沙箱 | 隔离的代码执行环境,防止影响宿主系统 |
| Seatbelt | macOS 沙箱机制 | macOS 内核级进程隔离技术 |
| Landlock | Linux 沙箱机制 | Linux 内核的安全模块,用于文件访问限制 |
| ReAct | 推理+行动 | LLM 边想边做的循环模式 |
| Agentic | 智能体化 | 具备自主决策、工具调用能力的特性 |
| IDE | 集成开发环境 | 如 VS Code、JetBrains 系列 |
| Composer | 多文件编辑器 | Cursor 的多文件同时编辑功能 |
| Cascade | 级联流 | Windsurf 的任务级多文件编辑流 |
| STT | 语音转文字 | 同 ASR |
| Streaming | 流式输出 | 边生成边输出,实时显示 |
| Whisper | OpenAI 语音识别模型 | 支持 99 种语言的开源 ASR 模型 |
| Function Calling | 函数调用 | OpenAI 的工具调用接口规范 |

---

## 一、CLI(命令行终端)

**全称:** Command Line Interface(命令行接口)

**定义:** 通过文本命令与 AI 系统交互的终端界面,是 AI Agent 最原生、最强大的交互形态。CLI Agent 可以直接访问本地文件系统、执行 Shell 命令、循环调用工具,是实现"Agentic(智能体化)"工作流的基础。

### 1.1 为什么 CLI 是 Agent 的核心入口

CLI 之所以成为 Agent 最原生的入口,源于其四大特性:

1. **本地执行权限:** 直接读写本地文件系统、运行 Shell 命令、调用系统级 API,这是 WebUI 和 Widget 无法比拟的。
2. **多步骤规划:** 自主拆解任务,循环执行"思考 → 工具调用 → 观察 → 再思考",这是 Agent Loop 的核心机制。
3. **上下文感知:** 自动扫描项目结构、读取相关文件、维护会话状态,具备项目级智能。
4. **工具调用:** 通过 Function Calling / MCP 调用外部能力,无限扩展能力边界。

### 1.2 CLI Agent 工作流程(Agent Loop 详解)

CLI Agent 的核心是 **Agent Loop(智能体循环)**,这是一个 while 循环,LLM 自主决定何时退出:

```
用户输入任务
    ↓
[初始化阶段]
    ├── Agent 解析意图
    ├── 扫描项目上下文(目录结构、配置文件)
    └── 加载项目规则(AGENTS.md / CLAUDE.md)
    ↓
[规划阶段](可选)
    └── 制定执行步骤(Plan)
    ↓
[执行循环] ←─────────────────────┐
    ├── [1] LLM 思考:分析任务,决定下一步  │
    ├── [2] 工具调用:选择并调用 Tool       │
    │   ├── 读文件(Read)                 │
    │   ├── 写文件(Write/Edit)            │
    │   ├── 执行命令(Bash)                │
    │   ├── 搜索(Grep/Glob)               │
    │   └── MCP 工具(自定义)              │
    ├── [3] 观察结果:把工具返回加入上下文  │
    ├── [4] 判断:任务完成?                │
    │   ├── 否 → 回到 [1] 继续思考 ────────┘
    │   └── 是 → 退出循环
    ↓
[输出阶段]
    └── 输出总结报告
```

### 1.3 三级权限控制(Approval Mode)

权限控制是 CLI Agent 安全的关键机制,通常分为三档(下表为 2025-2026 现行命名):

| 模式 | OpenAI Codex CLI | Anthropic Claude Code | 说明 | 适用场景 |
|------|------------------|----------------------|------|----------|
| **建议/规划模式** | `--sandbox read-only` + `-a untrusted` | `--permission-mode plan` | 只读分析或仅出计划,所有改动需确认 | 咨询、规划、答疑 |
| **自动编辑(工作区)** | `--sandbox workspace-write` + `-a on-request`(默认) | `--permission-mode acceptEdits` | 自动编辑文件,越界/危险操作需确认 | 日常开发、谨慎操作 |
| **完全自动** | `--full-auto`(≈ `-a on-failure`),或 `-a never` 完全不问 | `--permission-mode bypassPermissions`(或 `--dangerously-skip-permissions`) | 自主执行,无需确认(沙箱内) | 信任环境、批量任务 |

**命名变迁(重要):**

- **Codex CLI(Rust 版,2025 起):** 旧版(TypeScript 时代)的 `--approval-mode suggest/auto-edit/full-auto` 三档已改组为两个正交维度——`approval_policy`(`untrusted` / `on-failure` / `on-request` / `never`,CLI 参数 `-a` / `--ask-for-approval`)与 `sandbox_mode`(`read-only` / `workspace-write` / `danger-full-access`)。
- **Claude Code:** `--permission-mode` 的合法取值是 `default` / `acceptEdits` / `plan` / `bypassPermissions`(**不存在** `auto-edit` / `full-auto`);会话中可按 `Shift+Tab` 在 default → acceptEdits → plan 之间循环切换。

**关键概念:** Codex 的全自动仍运行在**内核沙箱**(macOS Seatbelt / Linux Landlock)内;Windows 原生环境沙箱能力有限,需要完整沙箱隔离请使用 WSL2。

### 1.4 主流 CLI Agent 工具对比(2026)

| 工具 | 维护方 | 形态 | 核心优势 | MCP 支持 | 安装命令 |
|------|--------|------|----------|----------|----------|
| **Codex CLI** | OpenAI | 纯 CLI(Rust) | 轻量、沙箱隔离、ChatGPT 生态 | ✅ | `npm i -g @openai/codex` |
| **Claude Code** | Anthropic | 纯 CLI(Node.js) | 深度 Agentic、Skills、Subagents、Hooks | ✅ | `npm i -g @anthropic-ai/claude-code` |
| **Gemini CLI** | Google | 纯 CLI | Gemini 模型集成 | ✅ | `npm i -g @google/gemini-cli` |
| **Copilot CLI** | GitHub | 独立 CLI(2025-09 新版) | GitHub 生态深度集成 | 部分 | `npm i -g @github/copilot` |
| **Aider** | 社区 | 纯 CLI | 开源、Git 友好 | 部分 | `pip install aider-chat` |
| **Cline** | 社区 | VS Code 插件 | 开源、可视化 | ✅ | VS Code 插件 |

### 1.5 CLI 上手 Checklist(从零到一)

以 Codex CLI 与 Claude Code 为例,一条完整的从零到一路径:

| 步骤 | Codex CLI | Claude Code |
|------|-----------|-------------|
| ① 前置 | 安装 Node.js 18+(`node -v` 验证) | 同左 |
| ② 安装 | `npm i -g @openai/codex` | `npm i -g @anthropic-ai/claude-code` |
| ③ 登录 | `codex login`(浏览器登录 ChatGPT 账号;无浏览器环境用 `codex login --device-auth`) | `claude login`(OAuth 登录 Claude 账号;也可设 `ANTHROPIC_API_KEY`) |
| ④ 最小任务 | 进入项目目录,`codex "看下这个项目结构并给出改进建议"` | `claude`,然后输入 "解释 src 目录的结构" |
| ⑤ 恢复会话 | `codex resume` | `claude -c`(继续上次)/ `claude -r`(选择恢复) |
| ⑥ 非交互(脚本/CI) | `codex exec "运行测试并修复"`(配 `--full-auto` 或 `-a never`) | `claude -p "..."`(配 `--output-format json`) |

**Windows 注意事项:**

- **Codex CLI:** 已支持原生 PowerShell 安装(`irm https://chatgpt.com/codex/install.ps1 | iex`),但内核沙箱在 Windows 原生环境下能力有限;要获得完整沙箱隔离(macOS Seatbelt / Linux Landlock 同级),建议在 **WSL2** 内安装使用。WSL2 无浏览器时登录用 `codex login --device-auth`。
- **Claude Code:** 支持 Windows 原生(PowerShell / CMD)与 WSL2;Hooks 的 shell 脚本默认经 Git Bash 执行,未安装 Git Bash 时可改用 PowerShell。
- 两者在 Windows 上都建议配合 **Windows Terminal** 获得完整 TUI 体验。

---

## 二、OpenAI Codex CLI

### 2.1 基本信息

- **官方文档:** https://developers.openai.com/codex/cli
- **GitHub:** https://github.com/openai/codex(83,200+ ⭐)
- **实现语言:** Rust(核心)+ TypeScript(周边)
- **配置文件:** `~/.codex/config.toml`
- **项目规则:** `AGENTS.md`(项目根目录)

### 2.2 核心特点

| 特点 | 说明 |
|------|------|
| **Rust 实现** | 启动快、内存占用低、跨平台 |
| **内核沙箱** | macOS seatbelt(`sandbox-exec`)/ Linux landlock 隔离文件访问 |
| **ChatGPT 生态** | 与 ChatGPT 订阅打通,支持 ChatGPT Plus/Pro/Team/Enterprise |
| **AGENTS.md 标准** | 项目级配置文件,跨工具通用 |
| **MCP 一等公民** | 通过 `codex mcp` 接入本地/远程 MCP Server |
| **Skills & Plugins** | 可复用指令 + 团队工具接入 |
| **多模型支持** | 支持 GPT-5、o3、o4-mini 等推理模型 |

### 2.3 十一大终端工作流(官方定位)

| 编号 | 功能 | 命令 | 说明 |
|------|------|------|------|
| 01 | 本地仓库循环 | `codex` | 在终端单循环内探索/编辑/运行本地工具 |
| 02 | Skills & Plugins | - | 可复用指令 + 团队工具接入 |
| 03 | Code Review | `codex "review this diff"` | 提交前的独立 review |
| 04 | 恢复会话 | `codex resume` | 恢复保存的会话 |
| 05 | 视觉输入 | `codex --image screenshot.png` | 视觉上下文输入 |
| 06 | 子智能体 | - | 委派聚焦任务给 subagent |
| 07 | 网页搜索 | `codex --search "query"` | 实时网页搜索 |
| 08 | MCP 集成 | `codex mcp` | 接入本地/远程 MCP Server |
| 09 | 权限设置 | `/permissions`(交互式) | 设置文件编辑/命令执行的审批边界 |
| 10 | 云端执行 | `codex cloud "task"` | 任务转交云端 |
| 11 | Shell 补全 | `codex completion bash` | shell 补全与主题 |

### 2.4 常用命令详解

```bash
# 启动交互式会话
codex

# 直接执行单次任务
codex "重构 utils.py 中的 parse_date 函数"

# 指定审批策略与沙箱(approval_policy × sandbox_mode)
codex -a never "运行所有测试并修复失败的"    # 全自动,不再询问
codex -a untrusted "..."                      # 每条命令都询问
codex --sandbox read-only "审查这个仓库"       # 只读沙箱
codex --full-auto "..."                       # 快捷参数:workspace-write 沙箱 + on-failure

# 使用特定模型
codex --model gpt-5 "分析项目结构"
codex -m o3 "..."

# 指定推理强度(仅推理模型)
codex --model o3 -c model_reasoning_effort="high" "..."

# 恢复上次会话
codex resume

# 图像输入(视觉上下文)
codex --image screenshot.png "解释这个 UI 截图"

# 网页搜索
codex --search "查一下 React 19 的新特性"

# MCP 操作
codex mcp list                    # 列出已配置的 Server
codex mcp add <name> -- <command> # 添加 Server
codex mcp remove <name>           # 移除 Server
codex mcp-server                  # 把 Codex 自身作为 MCP Server 暴露(stdio)

# 云端执行
codex cloud "在干净环境跑 CI"

# Shell 补全
codex completion bash > /etc/bash_completion.d/codex
codex completion zsh > ~/.zsh/completions/_codex
codex completion fish > ~/.config/fish/completions/codex.fish

# 权限管理(交互式)
/permissions

# 配置查看
codex config list
codex config set theme dark
```

### 2.5 ~/.codex/config.toml 完整配置示例

```toml
# ====== 模型配置 ======
model = "gpt-5"
model_reasoning_effort = "high"  # minimal / low / medium / high
model_provider = "openai"  # openai / anthropic / ollama 等

# ====== 审批与沙箱 ======
approval_policy = "on-request"  # untrusted / on-failure / on-request(默认)/ never
sandbox_mode = "workspace-write"  # read-only / workspace-write / danger-full-access
project_doc_max_bytes = 65536  # AGENTS.md 最大字节数

# ====== MCP Server 配置 ======
[mcp_servers.my-fs-server]
command = "python"
args = ["/path/to/server.py"]

[mcp_servers.remote-server]
url = "https://api.example.com/mcp"
env = { AUTH_TOKEN = "xxx" }

[mcp_servers.github]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-github"]
env = { GITHUB_PERSONAL_ACCESS_TOKEN = "ghp_xxx" }

# ====== 历史记录 ======
[history]
persistence = "save-all"  # save-all / none

# ====== Profiles(配置档)======
[profiles.fast]
model = "gpt-5-mini"
model_reasoning_effort = "low"

[profiles.safe]
approval_policy = "untrusted"
sandbox_mode = "read-only"

[profiles.coder]
model = "gpt-5"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# ====== 别名 ======
[aliases]
review = "-a untrusted '请审查当前 diff'"
test = "--full-auto '运行所有测试并修复失败的'"

# 使用: codex -p fast "..." 或 codex --profile safe "..."
```

---

## 三、Anthropic Claude Code

### 3.1 基本信息

- **官方文档:** https://code.claude.com/docs
- **GitHub:** https://github.com/anthropics/claude-code
- **实现语言:** Node.js / TypeScript
- **配置文件:** `.claude/settings.json`(项目级) + `~/.claude/settings.json`(用户级)
- **项目规则:** `CLAUDE.md`(项目根目录)
- **MCP 配置:** `.mcp.json`(项目根目录)

### 3.2 核心特点

| 特点 | 说明 |
|------|------|
| **深度 Agentic 工作流** | 强项在于多文件、多步骤复杂任务 |
| **Skills 系统** | 可复用的技能包,遵循 AgentSkills.io 标准 |
| **Subagents / Agent Teams** | 多 Agent 协作,内置 Explore/Plan/General-purpose |
| **Hooks 钩子** | 事件驱动自动化,100% 强制执行 |
| **CLAUDE.md** | 项目级行为配置,层级覆盖 |
| **MCP 一等公民** | 原生支持 MCP Client |
| **Plan Mode** | 仅规划不执行,适合复杂任务前置规划 |
| **Cost Tracking** | 内置成本追踪 |
| **Output Format** | 支持 JSON / Stream-JSON 输出 |

### 3.3 内置 Subagent(子智能体)

Claude Code 内置三种 Subagent,运行在独立上下文窗口中:

| Subagent | 模型 | 工具权限 | 用途 | 触发场景 |
|----------|------|----------|------|----------|
| **Explore** | 继承主对话(API 上限 Opus) | 只读(禁 Write/Edit) | 文件发现、代码搜索、代码库探索 | 需要理解项目结构时 |
| **Plan** | 继承主对话 | 只读 | Plan 模式下收集上下文 | 复杂任务前的规划阶段 |
| **General-purpose** | 继承主对话 | 全部工具 | 复杂多步任务(探索 + 修改) | 默认委派任务 |

> **版本变化:** v2.1.198 起 Explore 继承主对话模型(以前固定用 Haiku)。用户/项目级同名 `Explore` subagent 会覆盖内置。

调用 Explore 时,Claude 会指定**彻底程度**:quick / medium / very thorough。

### 3.4 自定义 Subagent 配置

**位置:** `.claude/agents/my-agent.md`

```markdown
---
name: code-reviewer
description: 深度代码审查,检查安全/性能/可维护性
model: haiku
tools: [Read, Grep, Glob]
---

你是资深代码审查专家。审查维度:
1. 安全性(注入、XSS、CSRF、权限)
2. 性能(N+1 查询、内存泄漏)
3. 可维护性(命名、复杂度)
```

### 3.5 Skills 系统

**位置:** `.claude/skills/`

```
.claude/skills/
└── code-review/
    ├── SKILL.md          # 技能定义
    ├── scenarios.md      # 场景示例
    └── templates/        # 模板文件
```

**SKILL.md 示例:**

```markdown
---
name: code-review
description: 执行深度代码审查,检查安全、性能、可维护性
trigger: "审查代码|code review|review code"
---

# 代码审查技能

## 审查维度
1. 安全性(注入、XSS、CSRF、权限)
2. 性能(N+1 查询、内存泄漏、阻塞调用)
3. 可维护性(命名、复杂度、重复代码)
4. 测试覆盖

## 输出格式
- 严重等级: 🔴 高 / 🟡 中 / 🟢 低
- 文件位置
- 问题描述
- 修复建议
```

### 3.6 Hooks 系统(生命周期钩子)

| 事件 | 触发时机 | 用途 |
|------|----------|------|
| `PreToolUse` | 工具调用前 | 拦截危险操作、修改参数、记录日志 |
| `PostToolUse` | 工具调用后 | 后置处理、结果验证、格式化 |
| `Notification` | 系统通知时 | 通知转发、告警 |
| `Stop` | Agent 结束时 | 清理资源、生成报告 |
| `SubagentStop` | 子 Agent 结束时 | 子任务结果处理 |

**Hooks 配置(`.claude/settings.json`,三层嵌套:事件 → matcher 组 → hooks 数组):**

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "/scripts/validate-bash.sh" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "/scripts/run-linter.sh" }
        ]
      }
    ]
  }
}
```

> **注意:** `matcher` 只负责"匹配哪个工具"(支持精确名、`A|B` 列表、正则),具体的处理程序要放在内层 `hooks` 数组里,且每个 handler 必须带 `"type": "command"`(或 `http` / `prompt`)。Claude Code **没有独立的 hooks.json 文件**,Hooks 就配在 settings.json 的 `"hooks"` 键下。

### 3.7 常用命令详解

```bash
# 启动交互式会话
claude

# 执行单次任务
claude -p "为 src/api/user.ts 添加单元测试"

# 恢复会话
claude --resume
claude -r

# 继续上次会话
claude --continue
claude -c

# MCP 管理
claude mcp list                              # 列出所有 Server
claude mcp add <name> -- <command> [args]    # 添加 Server
claude mcp add my-fs -- python server.py
claude mcp remove <name>                     # 移除 Server
claude mcp get <name>                        # 查看 Server 详情

# 模型选择
claude --model claude-sonnet-4
claude --model claude-opus-4

# 权限模式(合法取值:default / acceptEdits / plan / bypassPermissions)
claude --permission-mode plan                # 仅规划,不执行
claude --permission-mode acceptEdits         # 自动接受文件编辑
claude --permission-mode bypassPermissions   # 全自动,跳过所有权限确认
claude --dangerously-skip-permissions        # 等价的快捷参数(慎用,建议仅在沙箱/容器中)

# 成本管理
claude --cost

# 输出格式(用于脚本集成)
claude -p "..." --output-format json
claude -p "..." --output-format stream-json

# 设置
claude config set theme dark
claude config list

# 查看版本
claude --version
```

### 3.8 配置文件体系

| 文件/目录 | 作用 | 范围 |
|-----------|------|------|
| `CLAUDE.md` | 项目级行为规则(LLM 软约束) | 项目级 |
| `.mcp.json` | MCP Server 配置 | 项目级 |
| `.claude/agents/` | 自定义 Subagent 定义 | 项目级 |
| `.claude/skills/` | 自定义 Skill 定义 | 项目级 |
| `.claude/settings.json` | 项目设置(权限、Hooks 等) | 项目级 |
| `~/.claude/CLAUDE.md` | 用户级全局规则 | 用户级 |
| `~/.claude/settings.json` | 用户级全局设置 | 用户级 |

### 3.9 CLAUDE.md 层级覆盖机制

```
~/.claude/CLAUDE.md          # 用户级(全局,所有项目生效)
项目根/CLAUDE.md              # 项目级(覆盖用户级)
项目根/src/CLAUDE.md          # 子目录级(覆盖项目级)
```

**子目录 CLAUDE.md 优先级最高**。Claude Code 进入子目录时,会合并所有层级的 CLAUDE.md 内容,后定义的规则覆盖先定义的。

---

## 四、WebUI(网页交互界面)

**定义:** 在浏览器中运行的图形化 AI 交互界面。

### 4.1 典型代表

| 产品 | 厂商 | 网址 | 特色 |
|------|------|------|------|
| **ChatGPT** | OpenAI | https://chat.openai.com | 综合能力强,生态完善 |
| **Claude.ai** | Anthropic | https://claude.ai | 长上下文、Artifacts |
| **Gemini** | Google | https://gemini.google.com | 多模态、Google 生态集成 |
| **DeepSeek** | 深度求索 | https://chat.deepseek.com | 推理能力强、性价比高 |
| **通义千问** | 阿里 | https://tongyi.aliyun.com | 中文优化、阿里云集成 |
| **豆包** | 字节 | https://www.doubao.com | 中文场景、低价 |
| **Kimi** | 月之暗面 | https://kimi.moonshot.cn | 超长上下文(200万字,内测宣传值) |

### 4.2 特点

- **零门槛:** 无需安装,打开浏览器即用
- **图形化交互:** 支持文件上传、图片、插件、Artifacts
- **会话历史云端同步:** 跨设备访问
- **适合非技术用户:** 门槛最低
- **受限沙箱:** 无本地文件系统访问权限,执行能力受限

### 4.3 WebUI 的扩展形态

- **Artifacts(产物面板):** Claude.ai 首创,把生成的内容(代码、图表、文档)独立展示
- **Canvas(画布):** ChatGPT 的协同编辑界面
- **Projects(项目):** Claude 的项目级知识库 + 自定义指令
- **GPTs / Custom GPTs:** ChatGPT 的自定义 Agent 商店

---

## 五、Widget(嵌入式组件)

**定义:** 嵌入到其他应用中的轻量 AI UI 组件,不脱离宿主应用。

### 5.1 典型形态

| 形态 | 说明 | 典型代表 |
|------|------|----------|
| **聊天浮窗** | 网站右下角的 AI 助手按钮 | Intercom、Crisp |
| **侧边栏助手** | 应用内 AI 助手侧边栏 | Notion AI、Slack AI |
| **IDE 内嵌 Chat** | IDE 内嵌的 AI Chat 面板 | VS Code Copilot Chat |
| **文档站问答** | 文档站的 AI 问答组件 | Mintlify、Algolia AI |

### 5.2 特点

- **轻量嵌入:** 不脱离主应用,降低用户切换成本
- **上下文继承:** 可继承宿主应用状态(当前页面、选中文本等)
- **适合场景:** 客服、辅助、问答、文档检索
- **能力受限:** 受宿主应用沙箱限制,无法访问本地系统

### 5.3 Widget 技术栈

| 技术 | 说明 |
|------|------|
| **iframe 嵌入** | 最简单,跨域隔离 |
| **Web Components** | 标准、可复用 |
| **React/Vue 组件** | 流行框架,生态丰富 |
| **SDK 集成** | 厂商提供 JS SDK |

---

## 六、Copilot(代码副驾驶)

**定义:** IDE 内辅助编码的 AI 工具,深度集成开发环境,具备代码补全、Chat、代码生成、重构、解释等能力。

### 6.1 主流 Copilot 工具

| 工具 | 厂商 | 形态 | 核心优势 | 官网 |
|------|------|------|----------|------|
| **GitHub Copilot** | GitHub/Microsoft | VS Code 插件 | 代码补全最强、GitHub 生态 | https://github.com/features/copilot |
| **Cursor** | Anysphere | VS Code Fork | AI 原生编辑器、Composer | https://cursor.com |
| **Windsurf** | Codeium | VS Code Fork | Cascade 多文件流 | https://windsurf.com |
| **Trae** | 字节 | VS Code Fork | 国际版+国内版、MCP 支持 | https://www.trae.ai |
| **Claude Code + VS Code** | Anthropic | CLI + 编辑器联动 | Claude Code 深度 Agentic | https://code.claude.com |
| **JetBrains AI** | JetBrains | 内置 | JetBrains 生态集成 | https://www.jetbrains.com/ai/ |
| **Copilot CLI** | GitHub | 独立 CLI(2025-09 发布,取代已废弃的 `gh copilot` 插件) | 终端 Agent、GitHub 生态 | https://github.com/features/copilot/cli |

### 6.2 GitHub Copilot

**官网:** https://github.com/features/copilot

**核心能力:**

| 能力 | 说明 |
|------|------|
| **代码补全** | 行内、块级补全(最强项) |
| **Chat 对话** | 侧边栏对话,带代码库上下文 |
| **PR 摘要** | 自动生成 PR 描述 |
| **代码解释** | 选中代码问"这段代码做什么" |
| **代码重构** | 选中代码要求重构 |
| **测试生成** | 为选中代码生成单元测试 |
| **GitHub Actions 集成** | 与 CI/CD、Issues 联动 |
| **Copilot Workspace** | 任务级开发环境 |

### 6.3 Cursor

**官网:** https://cursor.com

**核心能力:**

| 能力 | 说明 |
|------|------|
| **Composer** | 多文件同时编辑,任务级 |
| **Chat** | 侧边栏对话,带代码库上下文 |
| **Cmd+K** | 行内代码生成与修改 |
| **@ mentions** | 引用文件、符号、文档、Web |
| **Tab 自动补全** | AI 增强的代码补全 |
| **Codebase Indexing** | 全代码库语义索引 |
| **多模型支持** | Claude、GPT、Gemini 等 |

### 6.4 Windsurf

**官网:** https://windsurf.com

**核心能力:**

| 能力 | 说明 |
|------|------|
| **Cascade** | 任务级多文件编辑流 |
| **Supercomplete** | 跨文件智能补全 |
| **Context Awareness** | 自动感知项目上下文 |
| **MCP 支持** | 原生 MCP 集成 |
| **Flow Actions** | 自动化工作流 |

### 6.5 Trae(字节跳动)

**官网:** https://www.trae.ai

**核心能力:**

| 能力 | 说明 |
|------|------|
| **国际版 + 国内版** | 国际用 Claude,国内用豆包 |
| **AI 原生编辑器** | 基于 VS Code |
| **Builder 模式** | 全自动开发模式 |
| **MCP 支持** | 原生集成 |
| **Skills 系统** | 内置 AI Skills |
| **Subagent** | 多智能体协作 |
| **多语言 UI** | 中英文界面 |

---

## 七、Chatbot(对话机器人)

**定义:** 以对话为核心交互模式的 AI 应用。

### 7.1 Chatbot 分类

| 类型 | 说明 | 典型代表 |
|------|------|----------|
| **通用聊天机器人** | 开放域对话 | ChatGPT、Claude、Gemini |
| **垂直领域机器人** | 特定领域专业对话 | 客服、法务、医疗、教育 |
| **任务型机器人** | 完成特定任务 | 订票、订餐、流程办理 |
| **角色扮演机器人** | 扮演特定角色 | Character.AI、Glow |
| **陪伴型机器人** | 情感陪伴 | Replika、Pi |

### 7.2 Chatbot 核心技术栈

```
┌─────────────────────────────────────┐
│  用户界面层(前端)                  │
│  Web / App / 小程序 / IM            │
├─────────────────────────────────────┤
│  对话管理层                         │
│  ├── 意图识别(Intent Recognition)  │
│  ├── 实体抽取(NER)                │
│  ├── 对话状态追踪(DST)            │
│  └── 对话策略(Policy)              │
├─────────────────────────────────────┤
│  能力层                             │
│  ├── LLM 对话能力                  │
│  ├── RAG 知识库检索                │
│  ├── 工具调用(Function Calling)   │
│  └── MCP 工具集成                  │
├─────────────────────────────────────┤
│  数据层                             │
│  ├── 会话状态管理                  │
│  ├── 短期记忆(对话历史)          │
│  └── 长期记忆(向量库)            │
└─────────────────────────────────────┘
```

### 7.3 Chatbot 平台

| 平台 | 类型 | 说明 |
|------|------|------|
| **OpenAI GPTs** | 通用 | 自定义 ChatGPT,可发布到 GPT Store |
| **Coze(扣子)** | 国内 | 字节的 Bot 开发平台 |
| **Dify** | 开源 | LLM 应用开发平台 |
| **FastGPT** | 开源 | 国产 RAG + 工作流平台 |
| **Voiceflow** | 国际 | 可视化对话流设计 |
| **Botpress** | 开源 | 企业级 Chatbot 平台 |

---

## 八、ASR(自动语音识别)

**全称:** Automatic Speech Recognition(自动语音识别)

**别名:** STT(Speech-to-Text,语音转文字)

**定义:** 将语音转换为文字的技术,是语音交互的输入端。

### 8.1 ASR 技术演进

| 时代 | 技术 | 代表 | 特点 |
|------|------|------|------|
| **传统时代** | HMM + GMM | Kaldi、Sphinx | 需要语言模型,效果一般 |
| **深度学习时代** | RNN/LSTM/CTC | DeepSpeech、Listen Attend Spell | 端到端,效果提升 |
| **Transformer 时代** | Transformer/Conformer | Wav2Vec 2.0、Hubert | 自监督预训练 |
| **大模型时代** | 多模态大模型 | Whisper、Gemini | 多语言、鲁棒性强 |

### 8.2 OpenAI Whisper

**官网:** https://openai.com/research/whisper
**论文:** "Robust Speech Recognition via Large-Scale Weak Supervision"(2022)
**开源仓库:** https://github.com/openai/whisper
**HuggingFace:** https://huggingface.co/openai/whisper-large-v3

**核心特点:**

| 特点 | 说明 |
|------|------|
| **多语言支持** | 99 种语言 |
| **开源可商用** | MIT License |
| **多任务** | 转录 + 翻译 + 语言识别 + VAD |
| **鲁棒性强** | 训练于 68 万小时多语言数据 |
| **变体丰富** | tiny/base/small/medium/large-v1/v2/v3 |

**Whisper 模型变体对比:**

| 模型 | 参数量 | 显存需求 | 速度 | 相对准确度 |
|------|--------|----------|------|-----------|
| tiny | 39M | ~1GB | 最快 | ★ |
| base | 74M | ~1GB | 很快 | ★★ |
| small | 244M | ~2GB | 快 | ★★★ |
| medium | 769M | ~5GB | 中 | ★★★★ |
| large-v1 | 1550M | ~10GB | 慢 | ★★★★☆ |
| large-v2 | 1550M | ~10GB | 慢 | ★★★★☆ |
| large-v3 | 1550M | ~10GB | 慢 | ★★★★★(2023-11,精度最高) |
| large-v3-turbo | 809M | ~6GB | 快(约 8 倍于 large-v3) | ★★★★☆(2024-10,精度略降) |

**Whisper 使用示例:**

```bash
# 安装依赖(两步缺一不可)
pip install openai-whisper        # Python 包
# 系统还必须安装 ffmpeg(新手第一大坑,缺了会报 "No such file or directory: 'ffmpeg'"):
#   Windows: winget install ffmpeg   (或下载 builds 手动加入 PATH)
#   macOS:   brew install ffmpeg
#   Ubuntu:  sudo apt install ffmpeg
```

```python
import whisper

model = whisper.load_model("large-v3")   # 无独显请改用 "small" 或 "base"
result = model.transcribe("audio.mp3", language="zh")
print(result["text"])
print(result["segments"])  # 带时间戳的分段
```

**faster-whisper 最小示例(纯 CPU 可跑,推荐入门):**

```python
# pip install faster-whisper(基于 CTranslate2,默认 int8 量化,速度约 4 倍)
from faster_whisper import WhisperModel

model = WhisperModel("small", device="cpu", compute_type="int8")
segments, info = model.transcribe("audio.mp3", language="zh")
for seg in segments:
    print(f"[{seg.start:.1f}s -> {seg.end:.1f}s] {seg.text}")
```

### 8.3 Whisper 衍生项目

| 项目 | 说明 | 仓库 |
|------|------|------|
| **Whisper.cpp** | C++ 实现,可在 CPU 上高效运行 | https://github.com/ggerganov/whisper.cpp |
| **faster-whisper** | CTranslate2 实现,4x 速度提升 | https://github.com/SYSTRAN/faster-whisper |
| **whisperX** | 增强版,带词级时间戳、说话人识别 | https://github.com/m-bain/whisperX |
| **WhisperLive** | 实时流式 ASR | https://github.com/collabora/WhisperLive |
| **WhisperKit** | Apple MLX 实现,iOS/macOS 优化 | https://github.com/argmaxinc/WhisperKit |

### 8.4 商业 ASR 服务

| 服务 | 厂商 | 特点 |
|------|------|------|
| **OpenAI Audio API** | OpenAI | `whisper-1`(基于 Whisper);2025-03 新增 `gpt-4o-transcribe` / `gpt-4o-mini-transcribe`,词错率更低 |
| **Google Speech-to-Text** | Google | 支持 125+ 语言 |
| **Azure Speech** | Microsoft | 企业级,带说话人识别 |
| **AWS Transcribe** | AWS | 与 AWS 生态集成 |
| **阿里云语音识别** | 阿里 | 中文优化 |
| **讯飞听见** | 讯飞 | 中文最强 |

### 8.5 ASR 应用场景

- **语音转文字:** 会议纪要、采访记录、字幕生成
- **语音助手输入:** Siri、Google Assistant、小爱同学
- **实时字幕:** 视频会议、直播
- **电话客服:** 自动转录通话
- **有声书 / 播客:** 文字稿生成
- **无障碍辅助:** 听障人士辅助

---

## 九、TTS(语音合成)

**全称:** Text-to-Speech(文本转语音)

**定义:** 将文字转换为语音的技术,是语音交互的输出端。

### 9.1 TTS 技术演进

| 时代 | 技术 | 代表 | 特点 |
|------|------|------|------|
| **拼接合成时代** | 拼接预录音片段 | 早期 TTS | 机械感强 |
| **参数合成时代** | HMM 参数合成 | HTS | 自然度提升 |
| **神经网络时代** | WaveNet、Tacotron | Google WaveNet | 接近人类 |
| **大模型时代** | 端到端多语言 | OpenAI TTS、Bark | 自然度高、可克隆 |

### 9.2 主流 TTS 模型

| 模型/服务 | 厂商 | 特点 | 状态 |
|-----------|------|------|------|
| **OpenAI TTS** | OpenAI | 自然度高,6 种音色 | 商业 API |
| **ElevenLabs** | ElevenLabs | 声音克隆最强 | 商业 |
| **Bark** | Suno | 开源,多语言,可克隆 | 开源 |
| **Tortoise TTS** | 社区 | 开源,质量高 | 开源 |
| **Coqui TTS** | Coqui | 开源,功能全面 | 开源(已停止维护) |
| **ChatTTS** | 社区 | 中文对话优化 | 开源 |
| **CosyVoice** | 阿里 | 多语言、情感可控 | 开源 |
| **Fish Speech** | 社区 | 多语言、零样本克隆 | 开源 |
| **Meta Voicebox** | Meta | 多任务 TTS | 论文发布 |
| **Microsoft TTS** | Microsoft | Azure Cognitive Services | 商业 |
| **Google TTS** | Google | Cloud Text-to-Speech | 商业 |

### 9.3 OpenAI TTS

**官方文档:** https://platform.openai.com/docs/guides/text-to-speech

**核心特点:**

| 特点 | 说明 |
|------|------|
| **自然度高** | 接近真人 |
| **6 种音色** | alloy、echo、fable、onyx、nova、shimmer |
| **流式输出** | 支持流式,低延迟 |
| **多语言** | 支持多种语言 |
| **多个模型** | tts-1(快)、tts-1-hd(高质量) |

**使用示例:**

```python
from openai import OpenAI
client = OpenAI()

response = client.audio.speech.create(
    model="tts-1-hd",
    voice="alloy",
    input="你好,欢迎使用语音合成服务。",
)
response.stream_to_file("output.mp3")
```

### 9.4 TTS 应用场景

- **语音助手播报:** Siri、小爱同学
- **有声书 / 播客生成:** 自动朗读文本
- **无障碍阅读:** 视障人士辅助
- **多语言配音:** 视频自动配音
- **客服 IVR:** 电话客服语音
- **游戏 NPC 语音:** 自动生成角色语音
- **教育配音:** 在线课程配音

### 9.5 语音全双工实时交互(2024-2025 范式转移)

把 §8(ASR)与 §9(TTS)拼起来,就是经典的**三段式语音链路**:

```
用户语音 → [ASR 转文字] → [LLM 生成文字] → [TTS 合成语音] → 播放
```

这条链路的问题:延迟高(每段各几百毫秒到数秒)、情感/语气在转文字时丢失、无法自然打断。**2024-2025 年,端到端语音模型(Speech-to-Speech)把三段合为一段**,成为语音入口的新范式:

| 维度 | 三段式(ASR→LLM→TTS) | 端到端语音(Realtime / Live) |
|------|----------------------|----------------------------|
| **延迟** | 秒级 | 数百毫秒,接近真人对话 |
| **情感语气** | 转文字后丢失 | 直接理解/生成语气、停顿、情绪 |
| **打断(Barge-in)** | 难以支持 | 原生支持,可随时插话 |
| **连接方式** | 多次 HTTP 调用 | 单条 WebSocket 全双工长连接 |
| **代表** | Whisper + GPT + TTS | OpenAI Realtime API、Gemini Live API |

**OpenAI Realtime API:**

- 模型:`gpt-realtime`(2025-08 GA;早期为 `gpt-4o-realtime-preview`)
- 接入:WebSocket `wss://api.openai.com/v1/realtime?model=gpt-realtime`(浏览器场景推荐 WebRTC,延迟更低)
- 音频流进流出(Base64 编码的 PCM16、单声道、24kHz),服务端 VAD 自动检测说话起止,支持函数调用与 MCP

最小接入示例(Python,`pip install websockets`,需环境变量 `OPENAI_API_KEY`):

```python
import asyncio, json, os, websockets

WS = "wss://api.openai.com/v1/realtime?model=gpt-realtime"

async def main():
    headers = {"Authorization": f"Bearer {os.environ['OPENAI_API_KEY']}"}
    async with websockets.connect(WS, additional_headers=headers) as ws:
        await ws.send(json.dumps({           # 配置会话:语音进语音出 + 服务端 VAD
            "type": "session.update",
            "session": {"type": "realtime", "output_modalities": ["audio"],
                        "audio": {"input": {"turn_detection": {"type": "server_vad"}},
                                  "output": {"voice": "alloy"}}},
        }))
        async for raw in ws:                 # 收到的音频增量可直接喂给播放器
            event = json.loads(raw)
            print(event["type"])             # response.audio.delta / input_audio_buffer.speech_started ...

asyncio.run(main())
```

> 注:`websockets>=13` 参数名为 `additional_headers`,旧版用 `extra_headers`;发送麦克风音频即向 `input_audio_buffer.append` 事件追加 Base64 音频块。旧预览模型 `gpt-4o-realtime-preview` 使用扁平 session 字段(`voice` / `turn_detection` 直接挂在 session 下),GA 版字段以官方文档为准。

**Gemini Live API:**

- 接入:WebSocket 双向流(`BidiGenerateContent`),推荐直接用官方 SDK:`pip install google-genai`
- 模型:`gemini-2.5-flash-native-audio-preview-09-2025`(原生音频,端到端)或 `gemini-live-2.5-flash-preview`(半级联)

```python
import asyncio
from google import genai

async def main():
    client = genai.Client()  # 读取 GOOGLE_API_KEY / GEMINI_API_KEY
    async with client.aio.live.connect(
        model="gemini-2.5-flash-native-audio-preview-09-2025",
        config={"response_modalities": ["AUDIO"]},
    ) as session:
        ...  # session.send_realtime_input(...) 发音频,session.receive() 收音频

asyncio.run(main())
```

**工程提示:** 生产级语音 Agent 不建议裸写 WebSocket——用 LiveKit Agents / Pipecat 等框架,它们封装了回声消除、打断处理、电话接入(SIP)与多模型切换。

---

## 十、流式输出(Streaming)

**定义:** 模型边生成边输出,用户可以实时看到生成过程,而不是等全部完成后才显示。

### 10.1 流式输出的实现方式

| 方式 | 全称 | 说明 | 适用场景 |
|------|------|------|----------|
| **SSE** | Server-Sent Events | HTTP 长连接,服务端推送 | Web 应用、LLM API |
| **WebSocket** | WebSocket | 全双工通信 | 实时交互、双向通信 |
| **HTTP Chunked** | HTTP 分块传输 | 简单分块 | 简单场景 |
| **gRPC Stream** | gRPC 流式 | 高性能 RPC | 微服务 |

### 10.2 SSE 详解(Server-Sent Events)

SSE 是 LLM API 流式输出最常用的协议,基于 HTTP 长连接:

```
GET /v1/chat/completions HTTP/1.1
Content-Type: application/json
stream: true

HTTP/1.1 200 OK
Content-Type: text/event-stream

data: {"id":"...","choices":[{"delta":{"content":"你"}}]}

data: {"id":"...","choices":[{"delta":{"content":"好"}}]}

data: [DONE]
```

### 10.3 流式输出的优势

| 优势 | 说明 |
|------|------|
| **用户体验好** | 可以看到实时进度,减少等待感 |
| **可中断** | 用户可以中途停止 |
| **节省时间** | 不需要等全部生成完 |
| **首 Token 快** | TTFT(Time To First Token)低 |
| **适合长输出** | 长文本生成不必等待 |

---

## 十一、扩展入口形态

除七大主要入口外,还有几种扩展形态:

### 11.1 IDE 集成(Integrated Development Environment)

**典型代表:** VS Code、JetBrains、Vim/Neovim

**集成方式:**
- **CLI 联动:** Claude Code 可与 VS Code 联动
- **原生插件:** GitHub Copilot、JetBrains AI
- **AI 原生 IDE:** Cursor、Windsurf、Trae

### 11.2 API 接口

**定义:** 提供 HTTP API 供开发者集成的入口形态。

**典型代表:**
- OpenAI API:https://platform.openai.com/docs
- Anthropic API:https://docs.anthropic.com/
- Google AI API:https://ai.google.dev/
- 阿里云百炼:https://bailian.console.aliyun.com
- 火山方舟:https://www.volcengine.com/product/ark

### 11.3 SDK(Software Development Kit)

**定义:** 厂商提供的开发包,封装 API 调用。

**主流 SDK:**

| SDK | 语言 | 厂商 |
|-----|------|------|
| `openai` | Python/JS | OpenAI |
| `anthropic` | Python/JS | Anthropic |
| `@google/generative-ai` | JS | Google |
| `langchain` | Python/JS | LangChain |
| `llamaindex` | Python/JS | LlamaIndex |
| `dashscope` | Python | 阿里 |
| `volcengine-python-sdk` | Python | 字节 |

### 11.4 桌面应用

- **ChatGPT Desktop:** OpenAI 推出的桌面应用
- **Claude Desktop:** Anthropic 推出的桌面应用
- **Raycast AI:** macOS 生产力工具
- **LM Studio:** 本地模型运行工具

### 11.5 移动应用

- **ChatGPT App:** iOS / Android
- **Claude App:** iOS / Android
- **Gemini App:** iOS / Android

### 11.6 浏览器扩展

- **Monica:** 多模型 AI 助手
- **Sider:** 侧边栏 AI
- **WebChatGPT:** 增强 ChatGPT 网页版

### 11.7 云端 Coding Agent(非本地入口)

2025 年起,主流厂商把 Coding Agent 从本地 CLI 搬到了**云端沙箱**:任务提交后在厂商的云环境里异步执行,直接产出 PR,本地只负责派单与审查。

| 产品 | 厂商 | 入口 | 特点 |
|------|------|------|------|
| **Codex Cloud** | OpenAI | chatgpt.com/codex | 任务在云端容器执行,与 GitHub 仓库打通,CLI 任务可用 `codex cloud` 转交 |
| **Jules** | Google | jules.google.com | 异步修 Issue、生成 PR,Gemini 驱动 |
| **Devin** | Cognition | devin.ai | "AI 软件工程师"定位,长任务自主执行 |
| **Copilot coding agent** | GitHub | github.com(把 Issue 指派给 Copilot) | 在 GitHub Actions 环境执行,直接开 PR |

**与本地 CLI 的取舍:** 云端适合"可异步、边界清晰"的任务(修小 bug、写测试、依赖升级),不占本地资源、天然隔离;本地 CLI 适合需要即时反馈、访问私有环境/机密的任务。两者常搭配使用:本地交互式探索 → 批量任务甩给云端。

---

## 十二、各入口对比与选型

### 12.1 综合对比表

| 入口形态 | 技术门槛 | 能力上限 | 本地权限 | 适用场景 | 代表产品 |
|----------|----------|----------|----------|----------|----------|
| **CLI** | 高 | 最高 | 全权限 | 开发、运维、复杂任务 | Codex CLI、Claude Code |
| **WebUI** | 低 | 中 | 受限沙箱 | 通用对话、内容创作 | ChatGPT、Claude.ai |
| **Widget** | 中 | 低 | 受限 | 客服、辅助问答 | 网站聊天浮窗 |
| **Copilot** | 中 | 高 | 部分 | 编码辅助 | Cursor、GitHub Copilot |
| **Chatbot** | 低 | 中 | 受限 | 客服、咨询、任务型 | 各类对话机器人 |
| **ASR+TTS** | 中 | 低 | 部分 | 语音助手、无障碍 | Whisper + OpenAI TTS |
| **API** | 高 | 高 | 无 | 开发者集成 | OpenAI API、Claude API |
| **SDK** | 高 | 高 | 无 | 开发者集成 | LangChain、LlamaIndex |

### 12.2 选型决策树

```
你的需求是什么?
│
├── 我要开发代码 / 运维任务
│   ├── 需要本地全权限 → CLI (Codex CLI / Claude Code)
│   ├── IDE 内辅助 → Copilot (Cursor / Windsurf / Trae)
│   └── 简单问答 → WebUI (ChatGPT)
│
├── 我要对话 / 内容创作
│   ├── 通用对话 → WebUI (ChatGPT / Claude.ai)
│   ├── 长文档处理 → WebUI (Claude 200K / Kimi 200万字)
│   └── 多模态 → WebUI (Gemini / GPT-4o)
│
├── 我要做客服 / 业务机器人
│   ├── 网站浮窗 → Widget (Intercom / Crisp)
│   ├── 复杂对话流 → Chatbot 平台 (Dify / Coze)
│   └── 任务型 → Chatbot + 工具调用
│
├── 我要语音交互
│   ├── 语音输入 → ASR (Whisper)
│   └── 语音输出 → TTS (OpenAI TTS / ElevenLabs)
│
├── 我要做产品集成
│   ├── 简单调用 → API (OpenAI / Anthropic)
│   └── 复杂应用 → SDK (LangChain / LlamaIndex)
│
└── 我要在 IDE 内
    ├── AI 原生 → Cursor / Windsurf / Trae
    ├── 插件式 → GitHub Copilot
    └── CLI 联动 → Claude Code + VS Code
```

---

## 十三、原始知识点速查

### 13.1 关键配置文件对照

| 文件 | 工具 | 位置 | 作用 |
|------|------|------|------|
| `config.toml` | Codex CLI | `~/.codex/` | 全局配置(模型、审批、MCP、别名) |
| `AGENTS.md` | Codex CLI / 通用 | 项目根 | 项目级行为规则(跨工具标准) |
| `CLAUDE.md` | Claude Code | 项目根 | 项目级行为规则 |
| `.mcp.json` | Claude Code | 项目根 | MCP Server 配置 |
| `.claude/skills/` | Claude Code | 项目根 | 自定义技能 |
| `.claude/agents/` | Claude Code | 项目根 | 自定义 Subagent |
| `.claude/settings.json` | Claude Code | 项目根 | 项目设置(权限、Hooks) |
| `~/.claude/CLAUDE.md` | Claude Code | 用户目录 | 用户级全局规则 |

### 13.2 CLI 工具横向对比(2026)

| 工具 | 形态 | 核心优势 | MCP 支持 | 安装 |
|------|------|----------|----------|------|
| **Codex CLI** | 纯 CLI(Rust) | 轻量、Rust 沙箱、ChatGPT 生态 | ✅ | `npm i -g @openai/codex` |
| **Claude Code** | 纯 CLI(Node) | 深度 Agentic、Skills、Subagents | ✅ | `npm i -g @anthropic-ai/claude-code` |
| **Gemini CLI** | 纯 CLI | Gemini 模型集成 | ✅ | `npm i -g @google/gemini-cli` |
| **Cursor** | IDE | 最成熟、VS Code 生态 | ✅ | 官网下载 |
| **Windsurf** | IDE | Cascade 多文件流 | ✅ | 官网下载 |
| **Trae** | IDE | 国际+国内、MCP | ✅ | 官网下载 |
| **Aider** | 纯 CLI | 开源、Git 友好 | 部分 | `pip install aider-chat` |
| **Cline** | VS Code 插件 | 开源、可视化 | ✅ | VS Code 插件 |
| **Copilot CLI** | 独立 CLI | GitHub 集成 | 部分 | `npm i -g @github/copilot` |

### 13.3 ASR / TTS 速查

| 任务 | 推荐模型 | 备注 |
|------|----------|------|
| 多语言 ASR | Whisper large-v3 | 99 种语言 |
| 中文 ASR | 讯飞 / Paraformer | 中文优化 |
| 实时 ASR | WhisperLive / faster-whisper | 低延迟 |
| 高质量 TTS | OpenAI TTS HD / ElevenLabs | 自然度最高 |
| 声音克隆 | ElevenLabs / Bark | 几秒样本克隆 |
| 中文 TTS | ChatTTS / CosyVoice | 中文优化 |
| 本地 TTS | Coqui TTS / Piper | 离线运行 |

---

## 十四、典型应用场景

### 14.1 软件开发场景

```
开发者
   ↓
[Cursor / Windsurf / Trae] ← IDE 内 Copilot
   ↓ 需要复杂任务
[Claude Code / Codex CLI] ← CLI Agent
   ↓ 需要工具扩展
[MCP Server] ← 接入外部能力
```

### 14.2 客服场景

```
客户
   ↓
[网站 Widget] ← 浮窗入口
   ↓ 复杂问题
[Chatbot 平台] ← Dify / Coze
   ↓ 需要业务系统
[API + 工具调用] ← 集成业务系统
```

### 14.3 语音助手场景

```
用户语音
   ↓
[ASR: Whisper] ← 语音转文字
   ↓
[LLM 对话] ← 理解 + 生成
   ↓
[TTS: OpenAI TTS] ← 文字转语音
   ↓
用户听到回复
```

---

## 十五、参考资源

### Codex CLI
- 官方文档:https://developers.openai.com/codex/cli
- 快速入门:https://developers.openai.com/codex/quickstart
- GitHub:https://github.com/openai/codex
- 配置参考:https://developers.openai.com/codex/cli/config

### Claude Code
- 官方文档:https://code.claude.com/docs
- 中文文档:https://docs.anthropic.com/zh-CN/docs/claude-code/overview
- GitHub:https://github.com/anthropics/claude-code
- Skills 文档:https://code.claude.com/docs/en/skills
- Subagents 文档:https://code.claude.com/docs/en/subagents
- Hooks 文档:https://code.claude.com/docs/en/hooks
- Memory 文档:https://code.claude.com/docs/en/memory

### AGENTS.md 标准
- 官网:https://agents.md

### Copilot 工具
- GitHub Copilot:https://github.com/features/copilot
- Cursor:https://cursor.com
- Windsurf:https://windsurf.com
- Trae:https://www.trae.ai

### ASR / TTS
- OpenAI Whisper:https://openai.com/research/whisper
- Whisper.cpp:https://github.com/ggerganov/whisper.cpp
- faster-whisper:https://github.com/SYSTRAN/faster-whisper
- whisperX:https://github.com/m-bain/whisperX
- OpenAI TTS:https://platform.openai.com/docs/guides/text-to-speech
- ElevenLabs:https://elevenlabs.io
- Bark:https://github.com/suno-ai/bark
- ChatTTS:https://github.com/2noise/ChatTTS
- CosyVoice:https://github.com/FunAudioLLM/CosyVoice

### LLM 应用平台
- Dify:https://dify.ai
- Coze:https://www.coze.com
- FastGPT:https://fastgpt.in
- LangChain:https://www.langchain.com
- LlamaIndex:https://www.llamaindex.ai

### API 提供商
- OpenAI API:https://platform.openai.com/docs
- Anthropic API:https://docs.anthropic.com/
- Google AI:https://ai.google.dev/
- 阿里云百炼:https://bailian.console.aliyun.com
- 火山方舟:https://www.volcengine.com/product/ark
